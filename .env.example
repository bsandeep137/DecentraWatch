# Required: JWT secret key for API authentication
JWT_SECRET=your_jwt_secret_here

# Required: Validator secret key
VALIDATOR_SECRET_KEY=your_validator_secret_key_here

# Optional: Reward amount per tick (default: 1)
REWARD_PER_TICK=1 